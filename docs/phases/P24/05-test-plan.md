# Phase P24: Kế Hoạch Kiểm Thử (Test Plan)

1. **Kiểm tra hợp đồng môi trường:** Unit test và script xác minh biến môi trường.
2. **Quét bí mật:** Kiểm tra không lộ Service Role trong source code và static build output.
3. **Smoke Tests:** Kiểm tra các route quan trọng (`/api/health`, `/manifest.webmanifest`, `/sw.js`, `/offline`, `/login`).
